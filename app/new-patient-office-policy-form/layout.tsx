import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "New Patient Office Policy Form | Roselle Center For Healing",
  description: "New patient office policy form for Roselle Center For Healing. Patient information and initial history.",
};
export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
