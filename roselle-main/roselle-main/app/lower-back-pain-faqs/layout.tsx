import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Lower Back Pain FAQs | Roselle Center For Healing",
  description:
    "Lower back pain FAQs — causes, chiropractic treatment, sciatica, and when to seek care. Roselle Center for Healing in Fairfax, VA.",
};

export default function LowerBackPainFaqsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
