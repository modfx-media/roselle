import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Back Pain Treatment in Fairfax, VA | Roselle Center for Healing",
  description: "Back pain treatment at Roselle Center for Healing in Fairfax, VA. Chiropractic adjustments, spinal manipulation, and holistic care for back pain relief.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
