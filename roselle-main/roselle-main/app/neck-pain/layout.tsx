import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Neck Pain Treatment | Chiropractor in Fairfax, VA | Roselle Center For Healing",
  description: "Neck pain treatment at Roselle Center for Healing in Fairfax, VA. Chiropractic adjustments, therapeutic exercises, and integrative health for neck pain relief.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
